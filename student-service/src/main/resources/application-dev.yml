server:
  port: 9091

spring:
  application:
    name: StudentService
  datasource:
    username: admin
    password: secret
    url: jdbc:mysql://localhost:3306/student

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka

